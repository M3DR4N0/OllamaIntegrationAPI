# Intentionally neutral.
# Keep `docker compose up` aligned with the base production-oriented setup.
# Use `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build`
# when you explicitly want the Development environment.

services: {}
